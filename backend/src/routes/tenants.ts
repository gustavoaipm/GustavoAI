import { Router } from 'express';

const router = Router();

// Placeholder for tenant routes
router.get('/', (req, res) => {
  res.json({ message: 'Tenant routes - to be implemented' });
});

export default router; 